# 15 · 综合练习与面试题库

> **本章目标**：5 个综合小项目；70 道面试题带要点；学习验收清单。
> **预计用时**：持续 · **难度**：★★★★

---

## 1. 学习验收（零基础 → 合格）

完成以下全部，说明已脱离「Python 0 基础」：

- [ ] 独立创建 venv、pip install、运行脚本
- [ ] 熟练使用 list、dict、str、for、if、def
- [ ] 读写 txt/csv，会用 try/except
- [ ] 写 100～200 行脚本结构清晰（函数拆分、main 入口）
- [ ] 会用 requests 调 API、pandas 读 CSV
- [ ] 能读懂带 class、装饰器、async 的示例代码
- [ ] 能口头解释 GIL、深拷贝浅拷贝、可变默认参数

---

## 2. 综合项目

### 项目 A · 待办命令行（难度 ★★）

**功能**：add / list / done / delete 子命令，数据存 `todos.json`。

**练到**：argparse、json、函数拆分、文件读写。

**参考结构**：

```python
# todo.py
import argparse
import json
from pathlib import Path

DATA = Path("todos.json")

def load():
    if not DATA.exists():
        return []
    return json.loads(DATA.read_text(encoding="utf-8"))

def save(items):
    DATA.write_text(json.dumps(items, ensure_ascii=False, indent=2), encoding="utf-8")

def cmd_add(text):
    items = load()
    items.append({"id": len(items) + 1, "text": text, "done": False})
    save(items)

# ... list / done / delete ...

def main():
    parser = argparse.ArgumentParser()
    sub = parser.add_subparsers(dest="cmd")
    p_add = sub.add_parser("add")
    p_add.add_argument("text")
    # ...
    args = parser.parse_args()
    # dispatch

if __name__ == "__main__":
    main()
```

---

### 项目 B · 日志分析（难度 ★★★）

读 nginx/access 风格日志，统计：总请求数、各状态码数量、IP Top 10。

**练到**：文件逐行、Counter、正则或 split。

---

### 项目 C · 天气查询 CLI（难度 ★★★）

调用公开天气 API（或 httpbin 模拟），城市名 → 打印 JSON 字段。

**练到**：requests、环境变量 API_KEY、异常处理。

---

### 项目 D · CSV 报表（难度 ★★★）

输入订单 CSV，输出：按城市汇总金额、按日期趋势（pandas groupby）。

**练到**：pandas、路径、写结果 csv。

---

### 项目 E · LLM 批量问答（难度 ★★★★）

读 `questions.txt` 每行一个问题，调 OpenAI API，结果写 `answers.md`。

**练到**：OpenAI SDK、循环、限速 sleep、logging。

---

## 3. 面试题库（70 题 · 要点）

### 3.1 基础（1～20）

| # | 问题 | 要点 |
|---|------|------|
| 1 | Python 特点？ | 解释型、动态类型、简洁、GIL |
| 2 | 缩进作用？ | 定义代码块 |
| 3 | list 和 tuple？ | 可变 vs 不可变 |
| 4 | dict 键要求？ | 可哈希、不可变 |
| 5 | `==` vs `is`？ | 值相等 vs 同一对象 |
| 6 | 深拷贝浅拷贝？ | copy.copy vs copy.deepcopy |
| 7 | 可变默认参数？ | 共享同一 list，用 None |
| 8 | `*args` `**kwargs`？ | 可变位置/关键字参数 |
| 9 | lambda 用途？ | 简单匿名函数 |
| 10 | 列表推导式？ | `[x for x in ... if ...]` |
| 11 | 生成器？ | yield，惰性，省内存 |
| 12 | 装饰器？ | 函数包装，@语法糖 |
| 13 | with 语句？ | 上下文管理器，自动清理 |
| 14 | `__init__`？ | 构造方法，不是创建对象本身 |
| 15 | self 含义？ | 实例自身引用 |
| 16 | 继承 super？ | 调父类方法 |
| 17 | 模块和包？ | .py 文件；含 __init__ 的目录 |
| 18 | venv 作用？ | 隔离依赖 |
| 19 | pip requirements？ | 锁定依赖版本 |
| 20 | `if __name__`？ | 直接运行 vs import 区别 |

### 3.2 进阶（21～45）

| # | 问题 | 要点 |
|---|------|------|
| 21 | GIL？ | 字节码互斥；CPU 多线程不并行 |
| 22 | 多线程 vs 多进程？ | IO vs CPU；内存隔离 |
| 23 | asyncio 适用？ | 高并发 IO，单线程协程 |
| 24 | 迭代器和可迭代？ | iter/next；for 协议 |
| 25 | 异常体系？ | try/except/else/finally |
| 26 | 自定义异常？ | 继承 Exception |
| 27 | json 模块？ | dumps/loads dump/load |
| 28 | pathlib 好处？ | 面向对象路径 |
| 29 | logging vs print？ | 级别、格式、生产 |
| 30 | argparse？ | CLI 参数解析 |
| 31 | dataclass？ | 减少样板数据类 |
| 32 | 类型 hints？ | 辅助 IDE，不强制运行时 |
| 33 | `@property`？ | 方法当属性访问 |
| 34 | 魔术方法 __repr__？ | 调试表示 |
| 35 | 鸭子类型？ | 有相同行为即可，不强类型名 |
| 36 | import 循环？ | 重构、延迟 import |
| 37 | 内存管理？ | 引用计数 + 循环垃圾回收 |
| 38 | 字符串不可变？ | 修改即新建对象 |
| 39 | join 为何快？ | 一次分配内存 |
| 40 | set 去重？ | 无序唯一 |
| 41 | Counter？ | 计数 most_common |
| 42 | defaultdict？ | 缺键自动 factory |
| 43 | 闭包？ | 内层函数引用外层变量 |
| 44 | LEGB？ | 变量查找顺序 |
| 45 | match case？ | 3.10 结构模式匹配 |

### 3.3 工程与 AI（46～70）

| # | 问题 | 要点 |
|---|------|------|
| 46 | requests timeout？ | 连接+读取超时 |
| 47 | raise_for_status？ | HTTP 错误转异常 |
| 48 | pandas 读 CSV？ | read_csv dtype 注意 |
| 49 | groupby 做什么？ | 分组聚合 |
| 50 | pytest fixture？ | 测试依赖注入（了解） |
| 51 | mock 用途？ | 隔离外部依赖（了解） |
| 52 | ruff/black？ | lint 与格式化 |
| 53 | mypy？ | 静态类型检查 |
| 54 | `.env` 管理密钥？ | python-dotenv，勿提交 git |
| 55 | 虚拟环境进 git 吗？ | 否，只提交 requirements |
| 56 | `__all__`？ | 控制 from pkg import * |
| 57 | 相对 import？ | `.` 和 `..` |
| 58 | wheel vs sdist？ | 二进制分发包 |
| 59 | CPU 密集 Python 加速？ | C 扩展、Cython、换多进程 |
| 60 | httpx vs requests？ | 支持 async |
| 61 | OpenAI SDK 流式？ | stream=True 迭代 chunk |
| 62 | RAG 在 Python 栈？ | LangChain/LlamaIndex 读文档 |
| 63 | Jupyter 适用？ | 探索不部署 |
| 64 | pickle 风险？ | 反序列化可执行恶意代码 |
| 65 | `if not list`？ | 空容器为 False |
| 66 | 整数缓存？-5~256 | 小整数单例（了解） |
| 67 | 列表 sort stable？ | 稳定排序 |
| 68 | 哈希表 dict 均摊 O(1)？ | 扩容 rehash |
| 69 | with 自定义类？ | __enter__ __exit__ |
| 70 | 为何学 Python？ | AI/脚本/自动化，配合 Java 后端 |

---

## 4. 推荐后续

| 方向 | 行动 |
|------|------|
| 巩固 | 重做 5 个项目，不看答案 |
| AI | [AI工程目录](../../05-AI工程/README.md) Java 实战 + 本库第 13 章 |
| Java 对照 | [附录-Java与Python对照](./99-附录-Java与Python对照.md) |
| 速查 | [00-速查总览](./00-速查总览.md) |

---

## 5. 恭喜

按 01～15 章走完，你已经**不是 Python 0 基础**。接下来靠**写小工具**维持手感：批处理文件、调 API、辅助 AI 实验。

---

← [14-并发异步与测试](./14-并发异步与测试.md) · [Python 目录](./README.md)
