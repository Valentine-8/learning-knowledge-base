# 第五章：LeetCode 题型与面试题库

> **阅读目标**：按题型体系刷题，掌握手写 LRU/TopK/生产者消费者，建立 7 年工程师 200+ 题错题本框架。
>
> **建议用时**：分 3–5 天通读 + 按题单刷；本文档作索引与模板库，非一次读完。

---

## 一、刷题战略（7 年工程师）

### 1.1 目标分层

| 层级 | 题量 | 标准 |
|------|------|------|
| 基础 | 80 | Hot 100 能独立中等题 30 min 内 |
| 进阶 | 150 | 剑指 Offer + 常见 DP/图 |
| 巩固 | 200+ | 二刷错题，Hard 能思路 |

**时间分配**：工作日 1–2 题 / 天，周末 3–4 题；**重总结轻数量**。

### 1.2 刷题流程

```
1. 限时 25–35 min（中等）
2. 无思路 → 看标签提示 → 仍不会看题解「思路」不看代码
3. 手写 AC → 写卡片：题型、模板、坑、复杂度
4. 三天后重做，仍卡 → 进错题本
```

### 1.3 题单优先级

| 顺序 | 题单 | 说明 |
|------|------|------|
| 1 | LeetCode Hot 100 | 覆盖面最广 |
| 2 | 剑指 Offer（国内） | 面试频率高 |
| 3 | Top 面试题 150 | 补充 |
| 4 | 按标签弱项专项 | 见下文分类 |

---

## 二、题型分类总表

| 大类 | 子类 | 代表题号 |
|------|------|----------|
| 数组 | 双指针 | 15, 11, 42 |
| 数组 | 滑动窗口 | 3, 76, 567 |
| 数组 | 前缀和/差分 | 560, 238 |
| 数组 | 矩阵 | 54, 48, 73 |
| 链表 | 基础操作 | 206, 21, 141 |
| 链表 |  hard | 25, 23, 146 |
| 二叉树 | 遍历 | 94, 102, 105 |
| 二叉树 | BST | 98, 230, 235 |
| 二叉树 | 路径/LCA | 236, 124, 437 |
| 堆 | TopK | 215, 347, 295 |
| 栈/队列 | 单调栈 | 739, 84, 85 |
| 回溯 | 组合排列 | 46, 78, 39 |
| 图 | BFS/DFS | 200, 207, 127 |
| 并查集 | 连通 | 547, 684 |
| 动态规划 | 线性 | 70, 198, 300 |
| 动态规划 | 背包 | 416, 322, 518 |
| 动态规划 | 区间 | 516, 312 |
| 贪心 | 区间/跳跃 | 435, 55, 56 |
| 二分 | 标准/答案 | 704, 34, 875 |
| 设计 | LRU/LFU | 146, 460 |
| 并发 | 交替/阻塞 | 1114, 1115（见 Java 并发） |

---

## 三、数组与字符串专题

### 3.1 双指针

| 题号 | 名称 | 要点 |
|------|------|------|
| 1 | 两数之和 | 哈希补数 |
| 15 | 三数之和 | 排序 + 去重 + 对撞 |
| 18 | 四数之和 | 两重循环 + 双指针 |
| 11 | 盛最多水 | 矮边移动 |
| 42 | 接雨水 | 双指针 / 单调栈 |
| 26 | 删除有序重复 | 快慢同向 |
| 75 | 颜色分类 | 荷兰国旗三指针 |
| 283 | 移动零 | 快慢 |

**三数之和模板**：

```java
Arrays.sort(nums);
for (int i = 0; i < n - 2; i++) {
    if (i > 0 && nums[i] == nums[i - 1]) continue;
    int l = i + 1, r = n - 1;
    while (l < r) {
        int sum = nums[i] + nums[l] + nums[r];
        if (sum == 0) { /* 记录 */ l++; r--; while 去重; }
        else if (sum < 0) l++;
        else r--;
    }
}
```

### 3.2 滑动窗口

| 题号 | 名称 | 窗口类型 |
|------|------|----------|
| 3 | 无重复最长子串 | 可变 |
| 76 | 最小覆盖子串 | 可变 + need |
| 567 | 排列 | 固定 len |
| 438 | 找异位词 | 固定 |
| 209 | 长度至少 K 的最短子数组 | 可变（前缀和+单调队列更优） |
| 424 | 替换后最长重复字符 | 可变 |

### 3.3 前缀和

| 题号 | 名称 |
|------|------|
| 560 | 和为 K 的子数组（HashMap 前缀和频次） |
| 525 | 连续数组（前缀和 + 最早下标） |
| 974 | 和可被 K 整除（同余前缀和） |

---

## 四、链表专题

### 4.1 必会模板

**反转、合并、判环、找中点、K 组反转** 必须闭卷。

### 4.2 题单

| 题号 | 名称 | 难度 |
|------|------|------|
| 206 | 反转链表 | 易 |
| 92 | 反转 II 区间 | 中 |
| 25 | K 组翻转 | 难 |
| 21 | 合并两个有序 | 易 |
| 23 | 合并 K 个 | 难（堆） |
| 141/142 | 环 | 易/中 |
| 160 | 相交 | 易 |
| 19 | 删除倒数第 N | 中 |
| 138 | 复制随机链表 | 中 |
| 148 | 排序链表 | 难（归并） |
| 234 | 回文链表 | 易 |
| 143 | 重排链表 | 中 |

### 4.3 K 组翻转思路

```
1. 测剩余长度是否 >= k
2. 反转当前 k 个（标准反转）
3. 递归或迭代连接下一段
4. dummy 头简化
```

---

## 五、二叉树专题

### 5.1 遍历必写

前序、中序、后序（递归+迭代）、层序 BFS。

### 5.2 题单

| 题号 | 名称 | 标签 |
|------|------|------|
| 104/111 | 深度 | 递归 |
| 226 | 翻转 | 递归 |
| 101 | 对称 | 递归 |
| 98 | 验证 BST | 上下界 |
| 230 | 第 K 小 | 中序 |
| 235 | LCA on BST | 方向 |
| 236 | LCA | 后序 |
| 124 | 最大路径和 | 贡献 |
| 437 | 路径和 III | 前缀和 Map |
| 297 | 序列化 | 前序 |
| 105/106 | 构造 | 分治 |
| 114 | 展平为链表 | 后序 |
| 199 | 右视图 | BFS/DFS 深度 |

---

## 六、堆与 TopK

| 题号 | 名称 | 解法 |
|------|------|------|
| 215 | 第 K 大 | 小顶堆 size=k |
| 347 | 前 K 频 | 堆 / 桶 |
| 295 | 数据流中位数 | 双堆（大顶+小顶） |
| 23 | 合并 K 链表 | 堆 |
| 703 | 数据流第 K 大 | 小顶堆 |
| 973 | 最接近原点 K 点 | 堆或快选 |

**数据流中位数双堆**：

```java
PriorityQueue<Integer> lo = new PriorityQueue<>(Comparator.reverseOrder()); // 低半大顶
PriorityQueue<Integer> hi = new PriorityQueue<>(); // 高半小顶

void addNum(int num) {
    lo.offer(num);
    hi.offer(lo.poll());
    if (lo.size() < hi.size()) lo.offer(hi.poll());
}

double findMedian() {
    return lo.size() > hi.size() ? lo.peek() : (lo.peek() + hi.peek()) / 2.0;
}
```

---

## 七、单调栈与单调队列

| 题号 | 名称 |
|------|------|
| 739 | 每日温度 |
| 496 | 下一个更大 I |
| 503 | 下一个更大 II（环：双倍数组） |
| 84 | 柱状最大矩形 |
| 85 | 最大矩形 |
| 239 | 滑动窗口最大值（单调队列） |

**84 题**：每柱作高，向左右扩；单调栈 O(n) 找左右第一小。

---

## 八、回溯专题

### 8.1 模板

```java
void backtrack(路径, 选择列表) {
    if (满足结束) { 结果.add(新路径); return; }
    for (选择 : 选择列表) {
        做选择;
        backtrack;
        撤销选择;
    }
}
```

### 8.2 题单

| 题号 | 名称 |
|------|------|
| 46 | 全排列 |
| 47 | 全排列 II（去重排序） |
| 78 | 子集 |
| 90 | 子集 II |
| 39/40 | 组合总和 I/II |
| 77 | 组合 |
| 79 | 单词搜索 |
| 51 | N 皇后 |
| 17 | 电话号码字母 |

**去重**：排序后 `if (i > start && nums[i]==nums[i-1]) continue`。

---

## 九、动态规划题单

### 9.1 线性

| 题号 | 名称 |
|------|------|
| 70 | 爬楼梯 |
| 198/213/337 | 打家劫舍 |
| 53/152 | 最大/乘积子数组 |
| 300/354 | LIS / 俄罗斯套娃 |
| 139/140 | 单词拆分 |
| 322/518 | 零钱 |
| 416/494 | 分割和 / 目标和 |
| 1143/72 | LCS / 编辑距离 |

### 9.2 股票

| 题号 | 交易次数 |
|------|----------|
| 121 | 1 次 |
| 122 | 无限 |
| 123 | 2 次 |
| 188 | k 次 |
| 309 | 含冷冻期 |

### 9.3 区间与其他

| 题号 | 名称 |
|------|------|
| 516 | 回文子序列 |
| 647 | 回文子串（中心扩展可 O(n²)） |
| 312 | 戳气球 |
| 91 | 解码方法 |
| 32 | 最长有效括号（栈/DP） |

---

## 十、图与并查集题单

| 题号 | 名称 |
|------|------|
| 200/695 | 岛屿数量/面积 |
| 207/210 | 课程表 |
| 547 | 省份数量 |
| 684/685 | 冗余连接 / III |
| 127 | 单词接龙 |
| 743 | 网络延迟 |
| 785 | 二分图 |
| 994 | 腐烂橘子 |
| 1091 | 二进制矩阵最短路 |
| 332 | 重新安排行程 |

---

## 十一、手写设计题（面试高频）

### 11.1 LRU Cache（146）

**要求**：get/put O(1)。

```java
class LRUCache {
    static class Node {
        int key, val;
        Node prev, next;
        Node(int k, int v) { key = k; val = v; }
    }
    private final int cap;
    private final Map<Integer, Node> map = new HashMap<>();
    private final Node head = new Node(0, 0), tail = new Node(0, 0);

    LRUCache(int capacity) {
        cap = capacity;
        head.next = tail;
        tail.prev = head;
    }

    private void remove(Node n) {
        n.prev.next = n.next;
        n.next.prev = n.prev;
    }

    private void insertHead(Node n) {
        n.next = head.next;
        n.prev = head;
        head.next.prev = n;
        head.next = n;
    }

    public int get(int key) {
        if (!map.containsKey(key)) return -1;
        Node n = map.get(key);
        remove(n);
        insertHead(n);
        return n.val;
    }

    public void put(int key, int value) {
        if (map.containsKey(key)) {
            Node n = map.get(key);
            n.val = value;
            remove(n);
            insertHead(n);
        } else {
            if (map.size() == cap) {
                Node lru = tail.prev;
                remove(lru);
                map.remove(lru.key);
            }
            Node n = new Node(key, value);
            map.put(key, n);
            insertHead(n);
        }
    }
}
```

**Follow-up**：线程安全 → `ConcurrentHashMap` + `ReentrantLock`；LFU 460 更复杂。

### 11.2 MinStack（155）

两个栈或一个栈存差值。

### 11.3 Trie（208）

见 02 章。

### 11.4 简易 LRU 对比 LinkedHashMap

```java
Map<Integer, Integer> cache = new LinkedHashMap<>(cap, 0.75f, true) {
    protected boolean removeEldestEntry(Map.Entry eldest) {
        return size() > cap;
    }
};
```

面试仍要求手写双向链表版。

---

## 十二、二分专题

| 题号 | 名称 | 类型 |
|------|------|------|
| 704 | 标准 | 基础 |
| 34/35 | 左右边界 | 变体 |
| 33 | 旋转数组 | 半有序 |
| 153/154 | 旋转最小 | 变体 |
| 875 | 吃香蕉 | 二分答案 |
| 1011 | 传送包裹 | 二分答案 |
| 410 | 分割数组最大和 | 二分答案 |

**二分答案模板**：若 `check(mid)` 单调，则 `while (lo < hi)` 缩区间。

---

## 十三、Java 并发类算法题（与 Phase3 联动）

| 题号 | 名称 | 考点 |
|------|------|------|
| 1114 | 按序打印 | synchronized / Semaphore |
| 1115 | 交替打印 FooBar | wait/notify 或 Lock |
| 1116 | 打印零偶奇 | 条件变量 |
| 1188 | 设计线程池 | 阻塞队列（了解） |

**交替打印**：

```java
class FooBar {
    private int n;
    private final Semaphore foo = new Semaphore(1);
    private final Semaphore bar = new Semaphore(0);

    FooBar(int n) { this.n = n; }

    public void foo(Runnable printFoo) throws InterruptedException {
        for (int i = 0; i < n; i++) {
            foo.acquire();
            printFoo.run();
            bar.release();
        }
    }

    public void bar(Runnable printBar) throws InterruptedException {
        for (int i = 0; i < n; i++) {
            bar.acquire();
            printBar.run();
            foo.release();
        }
    }
}
```

---

## 十四、Mock 面试题（口述 + 白板）

### 14.1 算法

1. 实现 LRU，分析复杂度。
2. 一亿个数找 Top 100（堆 / 分治 / 桶）。
3. 两个有序数组合并 K 个（堆）。
4. 字符串最长无重复子串。
5. 二叉树最近公共祖先。
6. 课程表能否修完。
7. 0-1 背包描述状态转移。
8. 快排 partition 手写。

### 14.2 数据结构原理（结合 Java）

1. HashMap 1.8 结构、扩容、树化。
2. ConcurrentHashMap 1.7 vs 1.8。
3. ArrayList vs LinkedList 场景。
4. fail-fast vs fail-safe。
5. 红黑树为何不用 AVL。

---

## 十五、错题本模板

```markdown
## LC 76 最小覆盖子串
- **标签**：#滑动窗口 #Hard
- **第一次**：2025-xx-xx，超时
- **坑**：window 计数应用 int[] 或 HashMap；收缩 while 不是 if
- **模板**：need/window + valid 变量
- **复杂度**：O(|s|+|t|)
- **二刷日期**：____ 结果：____
```

---

## 十六、时间复杂度速查

| n 范围 | 可接受复杂度 |
|--------|--------------|
| n ≤ 10 | O(n!) |
| n ≤ 20 | O(2^n) 状压 |
| n ≤ 500 | O(n³) |
| n ≤ 5000 | O(n²) |
| n ≤ 10⁵ | O(n log n) |
| n ≤ 10⁶ | O(n) |

---

## 十七、面试答题话术

**开场**：「我先确认数据规模和边界，再选暴力是否可行，然后优化到 XX 复杂度。」

**写完**：「时间 O(…)，空间 O(…)；可以进一步优化滚动数组但当前够用了。」

**不会**：「这题我想到 BFS，但状态定义还不清晰，能否给提示是 DP 还是图？」

---

## 十八、周计划示例（4 周）

| 周 | 主题 | 题量 |
|----|------|------|
| 1 | 数组链表栈队列 | 20 |
| 2 | 树堆 | 18 |
| 3 | 图 + 回溯 | 18 |
| 4 | DP + 贪心 + 设计 | 22 |

每周日复盘错题本，删除已熟练题。

---

## 十九、大厂 follow-up 习惯

- 数据流版本（第 K 大）
- 内存受限（外排序、分块）
- 多线程版本（LRU）
- 如果输入有序能否 O(n)（双指针）
- 返回所有解而非是否存在

---

## 二十一、Hard 题突破路径

| 阶段 | 题目 | 目标 |
|------|------|------|
| 1 | 42 接雨水、84 柱状图 | 单调栈熟练 |
| 2 | 239 滑动窗口最大、76 最小覆盖 | 窗口模板 |
| 3 | 124 最大路径和、968 监控二叉树 | 树形 DP |
| 4 | 312 戳气球、72 编辑距离 | 区间/二维 DP |
| 5 | 239 滑动窗口、460 LFU | 设计题 |

**Hard 策略**：先拆子问题 15 min；仍无思路看题解思路；隔日重写。

---

## 二十二、面试手写代码规范

```java
// 1. 类名方法签名
// 2. 边界：null、空数组
// 3. 变量名清晰 left/right, map, heap
// 4. 主逻辑
// 5. 口述复杂度
public int solution(int[] nums) {
    if (nums == null || nums.length == 0) return 0;
    // ...
}
```

- 不写无关 import
- 主函数可省略 main
- 需要 helper 时 private 方法

---

## 二十三、国内公司题型倾向

| 公司倾向 | 常见 |
|----------|------|
| 字节 | 数组、DP、二叉树 |
| 阿里 | 链表、并发、系统设计衔接 |
| 腾讯 | 字符串、图 BFS |
| 美团 | 滑动窗口、贪心 |
| 银行 | 基础数据结构、SQL 安全 |

以 **Hot 100 + 剑指** 覆盖 80% 即可，不必刷完 LeetCode。

---

## 二十四、与系统设计衔接的算法题

| 算法题 | 系统设计 |
|--------|----------|
| 146 LRU | 缓存组件 |
| 208 Trie | 搜索建议 |
| 355 设计推特 | 时间线 feed |
| 380 O(1) 数据结构 | 随机 get/delete |
| 295 数据流中位数 | 监控分位数 |

---

## 二十六、周复盘模板

```markdown
## 第 N 周算法复盘
- 本周完成：__ 题（新 __ / 复习 __）
- 薄弱标签：#区间DP #单调栈
- 代表错题：LC 84（原因：栈内维护下标不是高度）
- 下周计划：DP 背包 5 题 + 树路径 3 题
```

---

## 二十七、在线面试环境注意

- 确认语言 Java 版本
- 先讲思路再写码
- 跑通示例 + 自造边界
- 无 IDE 时手写 import 可省略
- 时间到写伪代码 + 复杂度亦得分

---

## 二十八、小结

```
分类刷 > 随机刷
Hot 100 + 剑指 > 题海
手写 LRU、反转链表、层序、LCA
错题本：标签 + 坑 + 二刷日期
与 Java 集合原理题联动准备
```

← [04-动态规划与贪心](./04-动态规划与贪心.md) · [00-速查总览](./00-速查总览.md) · [README](./README.md)
